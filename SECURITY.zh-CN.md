# 安全策略

[English](SECURITY.md)

## 支持版本

当前还没有版本化 Release。在首个 Release 定义支持窗口之前，安全修复只适用于公开
`main` 的当前状态。

## 报告方式

发现疑似漏洞时不要创建公开 Issue。请进入受影响仓库的 **Security** 标签页，选择
**Report a vulnerability**。报告中应包含提交、浏览器或运行环境、最小复现、影响，以及
用户数据是否离开浏览器。

不要在报告中包含真实密钥、私有应用素材或第三方数据。项目当前只有一位维护者，因此不
承诺固定响应时限，但所有报告都会在受影响版本发布前完成分流。

## 信任边界

- PNG 和 JPEG 是不可信输入，由浏览器负责解码。
- 编辑器拒绝大于 16 MiB 或 16,777,216 源像素的文件；Rust 还会独立将 Retina
  输出限制为 16,777,216 像素。
- 直接调用 WebAssembly 的调用方会在自己的浏览器内存中持有全部 PNG 与 ZIP
  字节，必须自行预留相应的 buffer 预算。
- 创建 ZIP 前，Rust 会验证布局对象。
- ZIP 成员名称固定，用户输入不能决定输出路径。
- 用户文件和生成字节只保存在浏览器内存中，直到用户下载。
- 托管应用没有上传路由、远程 API、分析脚本、Cookie 或持久化路径。
- 下游打包器必须把 ZIP 和 JSON 视为不可信输入，并将 macOS 签名和公证凭据放在本项目
  之外。

## 范围内问题

- 输入校验或资源限制绕过。
- 正式背景和审查预览未正确分离。
- ZIP 路径或契约违规。
- 用户数据发生非预期网络传输或持久化。
- 脚本注入、不安全 DOM 渲染或浏览器来源边界错误。

最终 DMG 创建、Finder 自动化、签名、公证、Worker Session 和 MCP transport 都没有
实现，因此不属于当前产品范围。
